from numpy import number


def in_str(short: str, long: str, buffer: int = None, decay: number = 0.7) -> bool:
    if buffer is None or buffer < 0:
        buffer = len(short) // 5
    i1, i2 = 0, 0

    u = long.index(short[0])
    if i1 < 0:
        return False

    for i in range(len(short)):
        if

print(3 / 2)

print("afafa"[0:2])